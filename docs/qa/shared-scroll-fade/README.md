Shared vertical viewport fade, 16 design points at both edges. Installed by BaseViewController; observes bounds, frame and content size without replacing delegates. Excludes text editors, paging/horizontal carousels, nested scrollers and existing masks. Category screenshots rendered using local test data. 35 focused tests passed (fade, resizing, scroll, keyboard/editor). Broad AdaptiveLayoutTests run additionally showed two Dynamic Type font-scaling failures; no font code changed in this task.
